# MongoDB Connection String
# Replace this with your actual MongoDB connection URL
# For local MongoDB: mongodb://localhost:27017/internfinder
# For MongoDB Atlas (cloud): mongodb+srv://username:password@cluster.mongodb.net/internfinder
MONGO_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/internfinder?retryWrites=true&w=majority


# JWT Secret Key - Used to sign and verify JWT tokens
# IMPORTANT: Change this to a random string in production
# You can generate a random string using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_super_secret_jwt_key_here_change_this_in_production

# Frontend URL - Used for CORS configuration
# For local development: http://localhost:5173
# For production (Vercel): https://your-frontend-app.vercel.app
# Can be comma-separated for multiple origins
FRONTEND_URL=http://localhost:5173,http://localhost:3000
